import 'package:flutter/cupertino.dart';

class LibraryDetails extends StatelessWidget {
  const LibraryDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Library Details'));
  }
}
